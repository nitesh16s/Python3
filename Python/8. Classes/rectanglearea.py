class Rectangle():

	def __init__(self, length, width):
		self.length = a
		self. width = b

	def area(self):
		return a*b

	def perimeter(self):
		return 2*(a+b)

a = float(input('Enter value of length: '))
b = float(input('Enter valueof width: '))

rectangle = Rectangle(a, b)

print('Area of rectangle is: ', rectangle.area())
print('Perimeter of rectangle is: ', rectangle.perimeter())